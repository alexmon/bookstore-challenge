# Notes

- bind .env file creates environmental variables on the container that cannot be overriden by phpunit event if phpunit force="true" exists
- same namespace imports: for some reason my editor LSP complains
